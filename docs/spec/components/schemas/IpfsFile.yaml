allOf:
  - $ref: '#/components/schemas/IpfsKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - url
        properties:
          url:
            type: string
            example: "ipfs://..."
