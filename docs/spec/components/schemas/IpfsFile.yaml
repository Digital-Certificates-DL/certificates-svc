allOf:
  - $ref: '#/components/schemas/IpfsFileUploadKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - url
        properties:
          url:
            type: string
            example: "ipfs://..."
