allOf:
  - $ref: '#/components/schemas/ParseUsersKey'
  - type: object
    required:
      - attributes
      - relationships
    properties:
      attributes:
        type: object
        required:
          - name
          - url
        properties:
            name:
              type: string
              example: "DL"
            url:
              type: string
              example: "https://google.sheet....."

