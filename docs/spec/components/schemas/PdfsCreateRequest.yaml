type: object
required:
  - data
  - url
  - address
properties:
  data:
    type: array
    items:
      anyOf:
        - $ref: '#/components/schemas/User'
          description: "user"
  url:
    type: string
    format: string
    example: "url"
  address:
    type: string
    format: string
    example: "bc3241dhf..."